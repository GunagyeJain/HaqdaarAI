# Windows symlink creation intermittently fails with EBUSY (antivirus / file indexer
# holding locks). A hoisted node_modules layout avoids symlinks entirely and is
# stable across Windows, macOS and CI.
node-linker=hoisted
